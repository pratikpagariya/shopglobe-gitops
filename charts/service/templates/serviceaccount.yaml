{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "service.name" .) .Values.serviceAccount.name }}
  labels: {{- include "service.labels" . | nindent 4 }}
{{- end }}
